import React, { FC } from 'react';
import { StyleSheet, View } from 'react-native';
import Header from './Header';

const LessonNotes: FC = ({props}: any) => {
    return(
        <View>
            <Header/>
        </View>
    )
} 

const styles = StyleSheet.create({

})

export default LessonNotes