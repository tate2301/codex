import * as React from 'react';
import {
  Dimensions,
  Animated,
  View,
  StyleSheet,
} from 'react-native';


const SPACING = 10;

const CodingReviewCard = ({ data }) => {
  return (
    <View style={styles.container}>
      
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    backgroundColor: '#fff',
  },
});

export default CodingReviewCard